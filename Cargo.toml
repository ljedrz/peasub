[package]
name = "peasub"
version = "0.1.0"
edition = "2021"
license = "MIT OR CC0-1.0"
description = "A metadata-private gossip protocol built on pea2pea, with constant-rate or Poisson-distributed cover traffic."
repository = "https://github.com/ljedrz/peasub"
readme = "README.md"
keywords = ["p2p", "gossip", "privacy", "metadata", "cover-traffic"]
categories = ["network-programming"]

[dependencies]
pea2pea = "0.56"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }
tokio-util = { version = "0.7", default-features = false, features = ["codec"] }
bytes = "1"
tracing = "0.1"
rand = "0.9"
parking_lot = "0.12"
thiserror = "2"
lru = "0.12"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
futures-util = "0.3"
